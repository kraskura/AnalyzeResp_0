
<div class="page-header">
  <h2 style="font-size: 30px; text-align:left;">Updates and To-Do List</h2>
  <p>Source: <a href = "https://github.com/kraskura/AnalyzeResp_0/blob/main/docs/progressNotes.Rmd" style = "color:gray;">/AnalyzeResp_0/docs/progressNotes.Rmd</a></p>
</div>



**To do list (not ordered by priority tasks):**


1. Reformat the functions into a package and create a packgdown.
2. Make return variables and save files locally only with permission.
3. Rename variables using more intuitive, descriptive terms.
4. Incorporate more flexibility in the types of input files or data that can be used to achieve the same final outcomes. 
5. Advance the development of SDA function.
6. Develop functions that estimate hypoxia tolerance, P[crit].
7. Expand the option for user-defined figure metrics (e.g., Add N=xxx in the quantile plots when estimating SMR/RMR in the `MMR_SMR_AS_EPOC` function)
8. Add analysis of Factorial Aerobic Scope (FAS = MMR / RMR)
9. Rename/expand on units when calculating MO~2~; i) animal (mgO~2~/min/animal) ii) mass-specific, per kg (mO~2~/min/kg), iii) standardized mass using scaling. 
10. Create a user-friendly example to kickstart the analysis. (e.g., fill in the blank type demo)
11. Make the function more accessible to those not using the FireSting oxygen sensor and those with the newest version files.

<br>

**Updates:**  

Date| Description | Previous | UPDATE
:--- |  :-------------  | :--- | :---
Feb-Mar 2022 | Update output file (...analyzed_MR.csv), previously all AS and MMR_overall were ignored when running the function with argument: `smrfile = "none"`
Jan 2022 | MMR function arguments, names changed| `Ch1`, `Ch2`, `Ch3`, `Ch4` | `mmr_Ch1`, `mmr_Ch2`, `mmr_Ch3`, `mmr_Ch4` 
Jan 2022 | MMR figure (new), analytical for displaying difference for sliding windows, slope and R^2^|  | 
Jan 2022 | MMR_SMR_AS_EPOC smoothing, fewers trends plotted, new argument for user input| NA  | `spar_levels` & better plot

